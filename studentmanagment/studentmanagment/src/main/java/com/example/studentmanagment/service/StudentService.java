package com.example.studentmanagment.service;

import com.example.studentmanagment.model.Course;
import com.example.studentmanagment.model.Student;

import java.util.List;
import java.util.Set;

//public interface StudentService {
    /*Student addStudent(Student student);
    List<Student> getAllStudents();
    Student getStudentById(Long id);
    Student updateStudent(Long id, Student updated);
    void deleteStudent(Long id);
    List<Student> searchStudentsByName(String name);
    Student enrollCourses(Long studentId, Set<Long> courseIds);
    Set<Course> getCoursesOfStudent(Long studentId);
} */